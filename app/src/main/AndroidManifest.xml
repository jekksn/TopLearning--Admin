<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ronit.toplearning">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AdminClasses.Login_Activity"
            android:windowSoftInputMode="adjustPan">
            android:label="@string/title_activity_login" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AdminClasses.Admin_Welcome_Activity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".AdminClasses.Password_Vergessen_Activity"
            android:windowSoftInputMode="adjustPan">
            android:label="@string/title_activity_password__vergessen_"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".AdminClasses.activity_registrieren"
            android:windowSoftInputMode="adjustPan" />


    </application>

</manifest>